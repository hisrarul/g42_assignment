#### docker-compose
```bash
# start container
docker-compose up -d

# stop container
docker-compose down

# pull docker images
docker-compose pull

# restart docker compose container 
docker-compose restart g42_app

```
