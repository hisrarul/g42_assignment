apiVersion: v1
kind: Service
metadata:
  name: {{ include "g42_app.fullname" . }}
  labels:
    {{- include "g42_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "g42_app.selectorLabels" . | nindent 4 }}
