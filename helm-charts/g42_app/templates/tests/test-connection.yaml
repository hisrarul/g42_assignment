apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "g42_app.fullname" . }}-test-connection"
  labels:
    {{- include "g42_app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "g42_app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
